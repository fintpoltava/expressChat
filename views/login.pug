extends layout

block content
    h2 Log In
    form(method='post', action='/login')
        div
            label Login:
            input(name='login', )

        div
            label Password:
            input(name='password', type='password')
        div
            input(type='submit', value='Login')
            p=message

